# 云边协同推理联调验证记录

> 联调方法 + 2026-08-10 实测结果（亚伦，本地 amqtt broker + 本地双服务）
> 配套脚本：`scripts/mqtt_cloud_sync_test.py`

## 一、联调链路

```
edge-agent                        Broker                        cloud-llm-service
  ├─ 事件融合 → task_router 决策卸载                                 │
  ├─ publish_inference_request ──► ward/W-01/node/{node}/           │ 订阅:
  │                                  inference/request  (QoS1)      │   ward/+/node/+/inference/request
  │◄── node/{node}/inference/response ◄─────────────────────────────┤  去重/幂等 → LLMClient 研判 → 回传
  └─ resolve(event_id,trace_id) → SQLite 写回 details.cloud_inference
```

- 主题：请求 `ward/{ward_id}/node/{node_id}/inference/request`；响应 `node/{node_id}/inference/response`
- 关联：`event_id + trace_id`；超时：边缘 `ROUTER_CLOUD_TIMEOUT_S`（默认 5s）
- 云端可用性统计：`GET /stats`（total_requests / total_responses / total_errors / pending_dedup）

## 二、联调环境（两种）

### 方式 A：Docker 全容器（推荐，贴近真实）

```powershell
docker compose up -d mqtt-broker cloud-llm-service edge-bed-01
# 验证云端已订阅
docker compose logs -f cloud-llm-service
curl http://localhost:8005/health
```

> 注：不要 `up` 全部服务——diffusion-service 声明了 NVIDIA GPU 资源，
> 无 GPU 机器会启动失败（见 docs/19 §A1）。

### 方式 B：本地轻量（无 Docker / Docker 未启动时）

```powershell
# 1) 依赖
pip install paho-mqtt==1.6.1 amqtt -i https://pypi.org/simple

# 2) 本地 broker（amqtt 纯 Python，1883，QoS 0/1）
python - <<'EOF'
import asyncio
from amqtt.broker import Broker
async def main():
    b = Broker()
    await b.start()
    await asyncio.sleep(3600)
asyncio.run(main())
EOF

# 3) cloud-llm-service（另开终端）
cd cloud-llm-service
MQTT_BROKER=localhost MQTT_PORT=1883 LLM_MODE=mock python -m uvicorn app.main:app --port 8004

# 4) edge-bed-01（另开终端；阈值调高让事件走云端路径）
cd edge-agent/src
WARD_ID=W-01 BED_ID=B01 EDGE_NODE_ID=EDGE-W01-B01 MQTT_BROKER=localhost \
MQTT_PORT=1883 CAMERA_MODE=mock LLM_MODE=mock TICK_SECONDS=3 \
ROUTER_EDGE_THRESHOLD=0.95 SCENARIO_PROFILE=fall_suspected,nurse_call python main.py
```

> Windows 环境变量写法用 `$env:KEY="value"` 多行 PowerShell；Git Bash 用上述 `KEY=val` 前缀。

## 三、7 场景联调（验收口径：每场景 ≥20 次）

| # | 场景 | 操作 | 通过判据 |
|---|---|---|---|
| 1 | 正常 | `python scripts/mqtt_cloud_sync_test.py --scenario normal --count 20` | 响应数=请求数，judgment 合法，trace 关联 |
| 2 | 重复 | `--scenario duplicate --count 20` | 20 次相同 event_id 仅 1 次响应（云端去重） |
| 3 | 超时 | 停 cloud-llm-service 后 `--scenario timeout --count 20` | 0 响应；边缘 DB 写 `fallback_edge/timeout` |
| 4 | 非法 | 边缘侧验证：云端返回非法 judgment 时写 `fallback_edge/invalid_judgment`（mock 不会产生，可临时改 LLMClient 或抓包改响应） | 边缘不崩溃、本地决策生效 |
| 5 | 未知 | 手动发指向不存在 event_id 的响应 | 边缘忽略，无副作用 |
| 6 | trace | 响应 trace_id 与请求不一致 | 边缘丢弃不匹配响应 |
| 7 | 断网 | `docker network disconnect` 边缘容器 → 恢复 | 断网期间本地自治；恢复后补传、云端重新响应 |

## 四、2026-08-10 实测结果（方式 B）

| 验证项 | 结果 | 证据 |
|---|---|---|
| 正常闭环 | ✅ 2/2 request→response，judgment=escalate（conf 0.5 mock 规则） | 脚本输出 + `/stats` |
| 边缘自动卸载 | ✅ 边缘场景事件自动上送云端，`/stats` 累计 6 请求 6 响应 0 错误 | `/stats` |
| 边缘写回 | ✅ SQLite `details.cloud_inference.status=completed`，judgment=confirm（P1+conf≥0.7），trace 关联 | `edge-agent/src/data/edge_EDGE-W01-B01.db` |
| 重复去重 | ✅ 同一 event_id ×3 仅计 1 次 | `/stats` 计数变化 |
| 超时回退 | ✅ 停云端后新事件 `status=fallback_edge, reason=timeout` | SQLite |

**结论**：云端 mock 链路与边缘写回闭环已实测打通，可进入与 P5 的真实 Broker 联调取证阶段。

## 四·补充、2026-08-19 七场景取证完成（每场景 ≥20 次，亚伦）

在本地真实 MQTT Broker（amqtt）上按第三节的 7 场景口径完成取证，全部通过，结构化证据见 `docs/evidence/mqtt-sync/`（`scenario1..7_*.json` + README.md），原始日志见 `.zcode/联调/7场景/`。

| # | 场景 | 次数 | 结果 | 关键实测 |
|---|---|---|:--:|---|
| 1 | 正常 | 20+28 | ✅ | 云端 20/20 响应 trace 关联；边端 28/28 `status=completed`、state=notified |
| 2 | 重复 | 20 | ✅ | 同 event_id×20：推理仅执行 1 次（requests+1）、19 次复用缓存（duplicates+19）、每请求均回响应 |
| 3 | 超时 | 28 | ✅ | 停云端后 3s 超时，28 条 `fallback_edge/timeout`；伴随自适应阈值 0.95→0.40 转边缘自治（自愈） |
| 4 | 非法 | 26 | ✅ | 注入 `judgment=bogus` 26 次，全部 `fallback_edge/invalid_judgment`，边缘不崩溃 |
| 5 | 未知 | 25 | ✅ | 注入不存在 event_id 响应 25 次，全部忽略（`status=unknown`），DB 零污染 |
| 6 | trace | 28 | ✅ | 注入 trace 篡改响应 28 次，全部丢弃（`status=trace_mismatch`），无 completed 误采纳 |
| 7 | 断网 | 24+ | ✅ | 断 broker 缓存 24 条（synced=0）→ 恢复后自动重连 `补传 39 条` → 54/54 synced=1 |

> 取证工具：`scripts/mqtt_cloud_sync_test.py`（normal/duplicate/timeout 等）+ 受控响应器 `controlled_responder.py`（invalid/wrongtrace/unknown 三模式）。
> 取证中修复：hybrid 请求 `request_mode` review→hybrid（667abbd）、cloud-backend 时间敏感测试（bba3242）、duplicate 判据修正。

## 五、取证建议

1. 每场景留存 `scripts/mqtt_cloud_sync_test.py` 输出 + `mosquitto_sub -t "#"` JSON
2. 边缘 SQLite 导出 `details.cloud_inference` 快照
3. 护士站前端事件卡片 ☁️云端/🔁协同 徽章截图（trace_id 标注）
4. 素材入库 `docs/evidence/mqtt-sync/`

---

## 八、2026-08-23~26 增量联调（真实 vLLM 链路 + 容错集成测试阶段1–6）

> 详细记录见 `docs/evidence/20260825_fault_test_stage1-6/容错集成测试-阶段0-6-总结.md` 与 `docs/evidence/20260823_vllm_real_link_5801d19/`。

### 8.1 真实 Qwen2.5-14B/vLLM 端到端链路（8/23）

- 拓扑：本机 edge-agent（`ff83e2a`）→ SSH 隧道 → AutoDL 裸机 mosquitto `:1883` → cloud-llm-service（vllm 模式，5801d19 代码，`:8005`）→ vLLM `:8501`（Qwen2.5-14B-Instruct-GPTQ-Int4）
- CLOUD：event `78012aed…` / trace `26982926…`，judgment=confirm，latency=390.5ms
- HYBRID：event `7d3d4414…` / trace `209a79c4…`，conf 0.70→0.85 复核提升，latency=545.4ms
- 四层证据（边缘日志/MQTT 抓包/SQLite/云端日志）trace 全程一致；vLLM HTTP 200 OK

### 8.2 容错集成测试阶段1–6（8/25–26，每场景 20 次）

| 阶段 | 注入方式 | 边缘行为 | SQLite 结果 |
|---|---|---|---|
| 1 重复 request 去重 | 20 相同 request | 云端 requests=1/duplicates=19/responses=20 | 未新增事件 |
| 2 重复 response 幂等 | 真实 response 原样重放 ×19 | 首条生效，19×`status=duplicate` 忽略 | 该 event 仅 1 条 |
| 3 非法 judgment | 拦截脚本回 `judgment=bogus` ×20 | 20×`回退边缘: invalid_judgment` | 20/20 fallback_edge，state=new |
| 4 未知 event | 对不存在 event_id 发响应 ×20 | （离线期发出）SQLite 落库 0 条；在线行为见阶段1（status=unknown） | 0 条 |
| 5 trace 不匹配 | 同 event_id 错误 trace-B 响应 ×20 | 20×`status=trace_mismatch` 拒绝 | completed=0 / fallback_edge=20 |
| 6 云端不可用+恢复 | 停 cloud-llm 无响应 | timeout worker 触发 25×`fallback_edge/reason=timeout`，事件未丢失；恢复后正常 completed，synced=0 遗留=0 | — |

附加观察：云端持续异常时 TaskRouter 自适应降低路由阈值（0.95→0.40），自动转本地自治。

### 8.3 边缘版本说明

本轮验证边缘端为 `p1/edge-timeout-fix` 系（本地分支 `edge-fault-test @ 54f0dbc`，含 status=timeout 识别与 P6 受控响应器）；云端 cloud-llm-service 为 `5801d19` 版本代码。

# 云端真实 vLLM 链路验证记录（2026-08-23 晚）

> 状态：✅ 端到端通过（边端 → AutoDL Broker → cloud-llm(vllm) → Qwen2.5-14B/vLLM → 回传）
> 本记录对应 **1 条新的正常 request_mode=cloud 请求**（timeout_ms=10000），未复用任何 mock 测试 ID。

## 一、链路拓扑

```
本机 Windows (edge-agent @ ff83e2a)          AutoDL 实例 (烽亮已准备)
┌─────────────────────────────┐    SSH 隧道   ┌──────────────────────────────────────┐
│ EDGE-W01-B02 (B02, mock场景) │ 1884→1883     │ mosquitto :1883（远端 Broker）         │
│  MQTT 127.0.0.1:1884 ───────┼──────────────►│   ├─ cloud-llm-service :8004 (vllm 模式)│
│  request_mode=cloud          │               │   │   └─ http://localhost:8501 ──► vLLM │
│  timeout_ms=10000            │               │   │        Qwen2.5-14B-Instruct-GPTQ-Int4│
└─────────────────────────────┘               └──────────────────────────────────────┘
```

- **边缘版本**：`ff83e2a fix(edge): 云端超时响应识别优先于 pending 状态检查`（master，含 status=timeout 识别）
- **云端代码**：`cloud-llm-service` 本地 master 版本同步至 AutoDL（修复远端旧版硬编码模型名与 completions 端点问题，改用 chat/completions + messages 格式）
- **模型**：Qwen/Qwen2.5-14B-Instruct-GPTQ-Int4（vLLM 0.10.1，GPTQ-Int4，`docs/23` 确认环境）
- **Broker**：AutoDL mosquitto `:1883`，经 SSH 隧道 `-L 1884:localhost:1883` 打通

## 二、运行配置

| 项 | 值 |
|---|---|
| 边缘节点 | EDGE-W01-B02（ward=W-01, bed=B02） |
| 场景 | SCENARIO_PROFILE=seizure（P1, conf=0.85） |
| 路由阈值 | ROUTER_EDGE_THRESHOLD=0.95（边缘得分 0.445 < 阈值 → 卸载云端） |
| 云端超时 | ROUTER_CLOUD_TIMEOUT_S=10 → timeout_ms=**10000** |
| 边缘 LLM | mock（本地语义增强）；推理研判走云端 vLLM |
| MQTT | 127.0.0.1:1884（隧道 → 远端 1883） |

## 三、证据（本次新 ID）

### 3.1 核心 ID
- **event_id**: `fd3496d4-5dc8-4845-8d40-cd7fbacdc7f6`
- **trace_id**: `2dcaf0e9-b722-4fad-8082-0a957d19cf4b`（请求/响应/云端日志/边端 SQLite 全程一致）
- 请求时间：2026-08-23 14:40:24.639Z；响应时间：14:40:25.094Z

### 3.2 MQTT 抓包（`capture.jsonl`）
- 请求 `ward/W-01/node/EDGE-W01-B02/inference/request`：`request_mode=cloud, timeout_ms=10000`
- 响应 `node/EDGE-W01-B02/inference/response`：
  - `judgment=confirm, confidence=0.85`
  - `latency_ms=507.5`（真实 vLLM 推理耗时）
  - `model_name=Qwen/Qwen2.5-14B-Instruct-GPTQ-Int4, model_version=gptq-int4`
  - advice：`立即检查患者状况，确保患者安全，必要时通知医生进行进一步评估和处理。`

### 3.3 边缘日志（`edge-real-vllm.log`）
```
[EDGE-W01-B02] ☁️ 卸载云端: seizure (边缘得分0.45<阈值0.95，复杂度高)
[EDGE-W01-B02] ☁️ 上报事件: seizure [P1] conf=0.85 route=cloud ttft=39ms
[EDGE-W01-B02] 收到消息: topic=node/EDGE-W01-B02/inference/response
[EDGE-W01-B02] 云端研判结果: event=fd3496d4-5dc8-4845-8d40-cd7fbacdc7f6, judgment=confirm, conf=0.85
[EDGE-W01-B02] 云端建议: 立即检查患者状况，确保患者安全，必要时通知医生进行进一步评估和处理。
```

### 3.4 云端 cloud-llm 日志（`cloud-llm-vllm.log.snippet`）
```
request_validated  | event fd3496d4… | trace 2dcaf0e9… | conf 0.85
inference_started  | mode vllm       | model Qwen/Qwen2.5-14B-Instruct-GPTQ-Int4
HTTP Request: POST http://localhost:8501/v1/chat/completions "HTTP/1.1 200 OK"   ← 真实 vLLM 调用成功
inference_completed| judgment confirm | conf 0.85 | latency 507.5
response_published
```

### 3.5 边端 SQLite（`edge_EDGE-W01-B02.db`，safety_events）
- `state=notified, synced=1`
- `details.routing = {target: cloud, reason: 边缘得分0.45<阈值0.95，复杂度高, network_state: connected}`
- `details.cloud_inference = {status: completed, judgment: confirm, confidence: 0.85, latency_ms: 507.5, trace_id: 2dcaf0e9…}`

## 四、备注

1. **云端代码同步**：AutoDL 原 `llm_client.py` 为旧版（硬编码 `qwen2.5-14b-awq`、调用 `/v1/completions`），与 vLLM chat API 不匹配导致 400 回退 mock；已替换为本地 master 确认版（chat/completions + messages），重测 200 OK。
2. 本次共产生两条真实链路事件（run1 `d0ec9670…`、run2 `fd3496d4…`），均为新 UUID；run2 作为本记录主证据。
3. 隧道命令：`ssh -N -L 1884:localhost:1883 -L 18501:localhost:8501 -L 18004:localhost:8004 -p 22165 root@connect.nmb1.seetacloud.com`（密码未写入任何日志/文档）。

## 五、本目录文件

- `capture-run1.jsonl` / `capture.jsonl`：MQTT 抓包（run2 为正式证据）
- `edge-real-vllm.log`：边缘日志
- `cloud-llm-vllm.log.snippet`：云端 cloud-llm + vLLM 调用日志
- `capture_mqtt.py`：抓包脚本

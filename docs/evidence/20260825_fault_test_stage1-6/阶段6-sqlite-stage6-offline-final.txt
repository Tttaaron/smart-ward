======================================================================
阶段6 云端不可用/超时回退 —— P1 侧核对结果（离线阶段）
======================================================================
流程: P6 停止 cloud-llm-service（Broker 保持在线）；P1 加速触发真实事件，
      边缘等不到响应，timeout worker 触发回退（两批）
查询时间: 2026-08-25 23:18:27 中国标准时间

[1] 边缘日志: 25 条 '云端不可用，回退边缘: reason=timeout'

[2] SQLite 落库状态:
    completed 落库: 0（应为 0）
    fallback_edge 落库: 25（25/25，本地边缘判断保留）
    无记录（事件丢失）: 0 → 事件未丢失 25/25
    synced 分布: synced=1 × 25 / synced=0 × 0

结论: 云端不可用时边缘 10s 超时回退（fallback_edge/reason=timeout），本地判断保留，
      SQLite 事件未丢失（25/25 落库）。→ 阶段6 离线超时回退 ✅
======================================================================

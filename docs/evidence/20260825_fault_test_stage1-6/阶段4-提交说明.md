# 阶段4（未知 event）P1 侧提交说明

> 边缘版本：`edge-fault-test @ 54f0dbc`（`54f0dbcf3721e3e33ea55ab2a1f02474d87a0175`）
> 节点：EDGE-W01-B02 ｜ 时间：2026-08-25（中国标准时间）

## 提交物

| 文件 | 说明 |
|---|---|
| `edge-unknown.log` | 边缘日志：随后的真实事件验证（seizure→cloud→confirm，`d32e82b7-…`），证明云端/边缘正常处理 |
| `capture-unknown.jsonl` | MQTT 抓包：真实事件的 request/response 记录 |
| `sqlite-stage4-unknown-final.txt` | **SQLite 查询结果（核心证据）**：20 个 `controlled-unknown-*` event_id 落库数为 **0** |

## 核心结论（对应任务书/P7 核对点）

1. **未知事件未进入正常完成路径**：20 个 `controlled-unknown-*`（P6 伪造 event_id）在 `safety_events` 中**均为 0 条**，没有被当作新事件落库、没有 `state=notified`、没有推到完成路径。
2. **不影响已知事件处理**：噪音之后触发的真实事件 `d32e82b7-a785-40dd-ae22-f5973eb9d4e8` 正常落库（`state=notified, synced=1, cloud_inference.status=completed`），云端 `/stats` `total_requests=1, total_errors=0`，未被 20 条噪音干扰。

## 说明（诚实标注）

P6 发出 20 条 `controlled-unknown-*` 响应时，P1 侧 edge-agent 处于离线状态（阶段3 结束后未运行），因此**没有逐条"收到并记 status=unknown"的边缘日志**；`edge-unknown.log`/`capture-unknown.jsonl` 记录的是噪音之后触发的真实事件验证。**SQLite 0 条落库**（`sqlite-stage4-unknown-final.txt`）是"未知事件未进入完成路径"的直接、可复核证据。

如需补充"边缘在线逐条接收并忽略 unknown"的日志，可让 P6 在 edge-agent 在线时重发一轮（阶段1 已实证该行为：未知事件响应 → `忽略云端响应: status=unknown`）。

# 云端真实 vLLM 链路验证记录（5801d19 版本，2026-08-23 晚重测）

> 状态：✅ 端到端通过（CLOUD + HYBRID 各 1 条，全部为全新 ID）
> 版本说明：本次请求由 **5801d19 版 cloud-llm-service（AutoDL :8005 实例）** 处理。
> 此前（23:20 前后）的证据由 AutoDL :8004 上运行的旧实例处理，不能作为 5801d19 的最终证据，已废弃（见 `docs/evidence/20260823_vllm_real_link/`）。

## 一、版本与部署

| 项 | 值 |
|---|---|
| 边缘 commit | `ff83e2a fix(edge): 云端超时响应识别优先于 pending 状态检查`（master，= `ff83e2a91da1ea29d50b92fd3952396c0476b8bf`）|
| 云端 commit | **`5801d19 merge(master): sync latest mainline changes`**（`p1/edge-timeout-fix` 分支，cloud-llm-service 代码由此导出部署）|
| 云端实例 | AutoDL `/root/autodl-tmp/cloud-llm-service-5801d19/`，uvicorn **:8005**（screen `cloudllm5801`）|
| 云端模式 | `LLM_MODE=vllm`，`CLOUD_LLM_MODEL_NAME=Qwen/Qwen2.5-14B-Instruct-GPTQ-Int4`，`CLOUD_LLM_MODEL_VERSION=gptq-int4`，`VLLM_ENDPOINT=http://localhost:8501/v1/chat/completions` |
| 唯一实例确认 | 重测前已杀掉 :8004 旧实例；重测后 `ss` 仅 :8005 一个 uvicorn；`/stats` = total_requests 2 / responses 2 / errors 0 |
| MQTT | 127.0.0.1:1884（SSH 隧道 → AutoDL mosquitto :1883）|

## 二、CLOUD 请求（seizure）

- **event_id**: `78012aed-11fa-431a-910c-975eaf1ac7de`
- **trace_id**: `26982926-911f-4e1e-a1d5-822bf5b4665e`
- 请求（`capture-cloud.jsonl`）：`request_mode=cloud, timeout_ms=10000`；reason=`边缘得分0.45<阈值0.95，复杂度高`
- 响应：`judgment=confirm, confidence=0.85, latency_ms=390.5, status=completed`
  - `model_name=Qwen/Qwen2.5-14B-Instruct-GPTQ-Int4, model_version=gptq-int4`
  - advice：`立即检查患者状况，准备必要的急救措施。`
- 边缘日志（`edge-cloud-5801.log`）：
  ```
  ☁️ 卸载云端: seizure (边缘得分0.45<阈值0.95，复杂度高)
  ☁️ 上报事件: seizure [P1] conf=0.85 route=cloud ttft=78ms
  云端研判结果: event=78012aed-11fa-431a-910c-975eaf1ac7de, judgment=confirm, conf=0.85
  ```

## 三、HYBRID 请求（fall_prediction）

- **event_id**: `7d3d4414-3b12-4063-9dac-7adee39f67c7`
- **trace_id**: `209a79c4-5bc4-45d1-a7ea-83a72d521936`
- 请求（`capture-hybrid.jsonl`）：`request_mode=hybrid, timeout_ms=10000`；reason=`P1事件置信度0.70<0.85，边缘+云端复核`
- 响应：`judgment=confirm, confidence=0.85`（云端复核将 0.70 提升至 0.85），`latency_ms=545.4, status=completed`
  - `model_name=Qwen/Qwen2.5-14B-Instruct-GPTQ-Int4, model_version=gptq-int4`
  - advice：`请立即检查患者状态，确认是否发生跌倒，并采取相应护理措施以确保患者安全。`
- 边缘日志（`edge-hybrid-5801.log`）：
  ```
  🔀 上报事件: fall_prediction [P1] conf=0.70 route=hybrid ttft=62ms
  云端研判结果: event=7d3d4414-3b12-4063-9dac-7adee39f67c7, judgment=confirm, conf=0.85
  ```

## 四、云端 5801d19 实例日志（`cloud-llm-5801.log.snippet`）

两条 trace 均出现完整阶段：`request_validated → inference_started(mode=vllm) → vLLM HTTP 200 OK → inference_completed(confirm, 390.5/545.4ms) → response_published`，`/stats` 确认 total_requests=2 / total_responses=2 / total_duplicates=0 / total_errors=0。

## 五、SQLite（`edge_EDGE-W01-B02.db`，safety_events）

| event_id | 模式 | state | conf | cloud_inference |
|---|---|---|---|---|
| 78012aed… | cloud | notified / synced=1 | 0.85 | completed, confirm, 390.5ms, trace 26982926… |
| 7d3d4414… | hybrid | notified / synced=1 | 0.70 | completed, confirm(0.85), 545.4ms, trace 209a79c4… |

## 六、本目录文件

- `capture-cloud.jsonl` / `capture-hybrid.jsonl`：MQTT 抓包
- `edge-cloud-5801.log` / `edge-hybrid-5801.log`：边缘日志
- `cloud-llm-5801.log.snippet`：5801d19 实例云端日志（含 vLLM 200 OK）
- `capture_mqtt.py`：抓包脚本
- `验证记录.md`：本文档
